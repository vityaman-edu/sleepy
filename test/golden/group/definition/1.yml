sleepy: |-
  (def a 1)
  (def b (sum 2 3))
  (def c (sum a b))

tafka: |-
  label(main):
  var(0): int = load(const(1): int): int
  var(1): int = load(const(2): int): int
  var(2): int = load(const(3): int): int
  var(3): int = sum(var(1): int, var(2): int): int
  var(4): int = sum(var(0): int, var(3): int): int

  var(5): int = load(const(0): int): int
  return var(5): int

asmik-virt: TODO
